
import React, { useContext, useEffect } from 'react';

const About = () => {
 
  return (
    <div>
      This is a Chat Application by using there user id and sender id which is taken from the database after signup. To do this you just need to signup through different emails and after login just fill the receiver Id which we can take from the database and vice versa.
    </div>
  )
}

export default About